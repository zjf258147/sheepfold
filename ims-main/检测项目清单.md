# IMS生产物料与产品追溯管理系统 — 完整检测清单

> 版本：v2.16（检测清单文档版本） | 更新日期：2026-09-20 | 适用项目版本：v2.17
>
> **本文件职责**：定义"应该检查什么、为什么检查、如何执行、通过标准"，不维护"最近一次是否通过"的实时状态。执行结果以 `PROJECT_STATE.md` 和 CI/测试报告为准。

---

## 执行顺序（按逻辑依赖排列）

```
前置步 → 端口可用性检查（check_port.ps1，确保 5173/8000 端口未被占用）
第0步 → API角色依赖检查（验证所有写操作端点使用 AllowWarehouseOrAbove / get_current_admin）
第1步 → 后端测试（Pytest，含 API 全量冒烟测试）
第2步 → 前端测试（Vitest）
第3步 → Skill 代码质量检查
第4步 → 专项测试补充（含前端路由遍历 / 多角色遍历）
第5步 → APK 打包检查（含打包脚本9步验证 / 关键文件检查 / 产物验证）
第6步 → 上线检查（含全局异常处理器验证）
第7步 → 数据字典更新
```

---

## 一、自动化测试

### 1.1 后端契约测试（Pytest）— `tests/contract/`

| # | 模块 | 文件 | 检查项 | 状态 |
|---|------|------|--------|:---:|
| 1 | 认证 | `test_auth_contract.py` | 登录/失败、Token刷新、登出 | ✅ |
| 2 | 用户 | `test_user_contract.py` | CRUD、角色权限、分页 | ✅ |
| 3 | 产品SKU | `test_product_contract.py` | 物料/成品CRUD、分类、BOM | ✅ |
| 4 | 合作伙伴 | `test_partner_contract.py` | 供应商/客户CRUD | ✅ |
| 5 | 来料管理 | `test_incoming_contract.py` | 到货/检验/退货/导出导入 | ✅ |
| 6 | 入库 | `test_inbound_contract.py` | 入库单、状态流转、库存 | ✅ |
| 7 | 出库 | `test_outbound_contract.py` | 出库单、拣货确认 | ✅ |
| 8 | 发货 | `test_shipment_contract.py` | 发货单CRUD、物流 | ✅ |
| 9 | RMA返修 | `test_rma_contract.py` | 退货/诊断/维修/报废/导出 | ✅ |
| 10 | BOM | `test_bom_contract.py` | BOM创建/编辑/版本 | ✅ |
| 11 | 库存 | `test_inventory_contract.py` | 查询/变动/快照 | ✅ |
| 12 | 审计 | `test_audit_contract.py` | 日志查询/操作记录 | ✅ |
| 13 | 仪表盘 | `test_dashboard_contract.py` | 统计接口 | ✅ |
| 14 | 打印 | `test_print_contract.py` | 6种单据打印接口 | ✅ |
| 15 | 设置 | `test_settings_contract.py` | 配置管理 | ✅ |
| 16 | 快照 | `test_snapshot_contract.py` | 库存快照/定时任务 | ✅ |
| **17** | **场站** | `test_station_contract.py` | **Station CRUD、分页、筛选（二期）** | ✅ 11用例 |
| **18** | **设备台账** | `test_device_ledger_contract.py` | **DeviceLedger CRUD、按SN/场站查询、质保期（二期）** | ✅ 12用例 |
| **19** | **盘点** | `test_stocktake_contract.py` | **Stocktake 创建/扫码/完成/取消（二期）** | ✅ 11用例 |
| **20** | **库存调整** | `test_inventory_adjustment_contract.py` | **InventoryAdjustment 确认/查询（二期）** | ✅ 6用例 |
| **21** | **客户扩展** | `test_customer_contract.py` | **Customer 新增8字段读写（二期）** | ✅ 3用例 |

### 1.2 后端场景测试（Pytest）— `tests/scenario/`

| # | 场景 | 流程 | 状态 |
|---|------|------|:---:|
| 1 | S001 | 原材料到货→检验→入库 | ✅ |
| 2 | S002 | RMA返修全流程（退货→诊断→维修→返厂→入库） | ✅ |
| 3 | S003 | 成品出库→发货 | ✅ |
| 4 | S004 | 原料退货流程 | ✅ |
| 5 | S005 | 原材料退货（不合格品处理） | ✅ |
| 6 | S006 | 报废审批（申请→审批→通过/驳回） | ✅ |
| **7** | **S007** | **设备出库→安装到场站→记录台账（二期）** | ✅ |
| **8** | **S008** | **设备退回→质保判断→维修→再发出（二期）** | ✅ |
| **9** | **S009** | **全面盘点→差异→调整（二期）** | ✅ |
| **10** | **S010** | **循环盘点→差异→调整（二期）** | ✅ |

### 1.3 后端边界测试（Pytest）— `tests/contract/test_edge_cases.py`

| # | 类别 | 检查项 | 状态 |
|---|------|--------|:---:|
| 1 | 打印API | 不存在(99999)、未授权(401)、无效类型(422) | ✅ |
| 2 | 分页 | 页码0/负数/超大/空结果 | ✅ |
| 3 | 数据校验 | 空字段、超长、特殊字符 | ✅ |
| 4 | SQL注入 | 恶意SQL片段 | ✅ |
| 5 | 并发状态 | 重复提交、状态冲突 | ✅ |
| **6** | **质保期** | **warranty_start>end、当天到期、NULL值（二期）** | ✅ |
| **7** | **盘点** | **空仓库范围、0个SN、全面盘点期间出入库（二期）** | ✅ |
| **8** | **Station** | **删除有活跃台账的场站（二期）** | ✅ |
| **9** | **DeviceLedger** | **removed_date<installed_date（二期）** | ✅ |
| **10** | **序列化异常** | **datetime/Decimal/None 等不可序列化字段返回时 500（二期）** | ✅ 8用例 |

### 1.4 前端单元测试（Vitest）— `src/__tests__/`

| # | 模块 | 文件 | 用例 | 状态 |
|---|------|------|:---:|:---:|
| 1 | 日期时间 | `datetime.test.js` | 8 | ✅ |
| 2 | 打印API | `print.test.js` | 4 | ✅ |
| 3 | 金额大写 | `amountToChinese.test.js` | 23 | ✅ |
| 4 | Vue组件 | `component.test.js` | 8 | ✅ |
| 5 | Pinia Store | `stores.test.js` | 20 | ✅ |
| 6 | API封装 | `api.test.js` | 4 | ✅ |
| 7 | 路由 | `router.test.js` | 12 | ✅ |
| 8 | 打印渲染 | `print-render.test.js` | 41 | ✅ |
| **合计** | | | **124** | ✅ |
| **9** | **质保期计算** | 未创建 | **0** | ❌ |
| **10** | **盘点差异** | 未创建 | **0** | ❌ |
| **11** | **设备台账工具** | 未创建 | **0** | ❌ |
| **12** | **轮询通知** | 未创建 | **0** | ❌ |
| **13** | **内网地址配置** | 未创建 | **0** | ❌ |

### 1.5 后端单元专项测试（Pytest）— `tests/unit/` + `tests/security/`

| # | 模块 | 文件 | 用例 | 状态 |
|---|------|------|:---:|:---:|
| 1 | 库存扣减 | `test_inventory_deduction.py` | 3 | ✅ |
| 2 | 状态机 | `test_state_machine.py` | 5 | ✅ |
| 3 | 编号生成 | `test_sequence.py` | 15 | ✅ |
| 4 | 权限矩阵 | `test_permission_matrix.py` | 15 | ✅ |
| 5 | SN关联 | `test_scenario_mainline_b.py`（含断言） | 1 | ✅ |
| **合计** | | | **39** | ✅ |
| **6** | **质保期计算** | `test_phase2_unit.py`（二期） | **6** | ✅ |
| **7** | **盘点差异** | `test_phase2_unit.py`（二期） | **5** | ✅ |
| **8** | **DeviceLedger关联** | `test_phase2_unit.py`（二期） | **5** | ✅ |
| **9** | **盘点锁库存** | `test_phase2_unit.py`（二期） | **4** | ✅ |
| **10** | **权限矩阵 v2** | `test_permission_matrix_v2.py`（二期） | **312** | ✅ |
|  | _参数化矩阵_ | `test_write_permission_matrix` | 264 | _6角色×44权限键_ |
|  | _Admin快速检查_ | `test_admin_always_passes` | 44 | _ADMIN所有操作不返回403_ |
|  | _Staff快速检查_ | `test_staff_always_denied` | 44 | _STAFF所有写操作返回403_ |

**运行命令：**
```bash
cd backend && python -m pytest tests/ -v --tb=short   # 后端全量
cd frontend && npm run test                             # 前端全量
```

### 1.6 后端 API 全量冒烟测试（Pytest）— `tests/smoke/`

> **目标**：四层断言体系，分层验证路由存在、参数校验、权限校验、业务逻辑。
> **测试文件**：`tests/smoke/test_api_smoke_all.py`（统一文件，覆盖所有端点）
>
> | 层 | 断言规则 | 说明 |
> |:---:|------|------|
> | 层1 | `status ∈ {200,201}` | 路由存在性，404 直接失败 |
> | 层2 | `status ∉ {500,502,503}` | 路径参数/query参数/文件下载端点，允许 400/404/422 |
> | 层3 | 允许 422 | POST/DELETE 端点参数校验，空 body 发送 |
> | 层4 | 完整参数创建/修改 | 业务逻辑验证，seed 数据创建实体后测试 |
>
> | 模块 | 端点数 | 方法 |
> |------|:---:|------|
> | auth / users / products / partners | ~16 | GET/POST/PUT/DELETE |
> | incoming / inbound / outbound / shipment | ~21 | GET/POST/PUT/DELETE |
> | rma / bom / inventory / audit | ~19 | GET/POST/PUT |
> | dashboard / print / settings / snapshot | ~15 | GET/POST/PUT |
> | station / device-ledger / stocktake / adjustment / customers（二期） | ~23 | GET/POST/PUT/DELETE |
>
> **运行命令**：
> ```bash
> cd backend && uv run pytest tests/smoke/test_api_smoke_all.py -v --tb=short
> ```

---

## 二、Skill 代码质量检查

> 对应宪法规则12：每次 `git commit` 前必须通过。按顺序执行。

### 2.1 代码质量检查（后端正交）

| # | Skill | 用途 | 触发条件 | 通过标准 | 状态 |
|---|-------|------|----------|----------|:---:|
| ① | `code-quality-skill` | Python命名/类型/异常/安全/日志 | 后端代码有修改 | 无 WARNING 以上 | ✅ |
| ② | `detect-code-smells` | 代码异味（Long Method/Duplicate Code等） | 新增/修改 >50行 | 无异味需重构 | ✅ |
| ③ | `code-review-and-quality` | 五轴审查（正确性/可读性/架构/安全/性能） | 合并前/重大改动 | 五轴通过 | ✅ |

### 2.2 技术栈专项检查（按修改范围触发）

| # | Skill | 技术栈 | 触发条件 | 状态 |
|---|-------|--------|----------|:---:|
| ④ | `fastapi-python` | FastAPI | 修改 API 路由/服务层 | ✅ |
| ⑤ | `sqlalchemy-alembic-expert-best-practices-code-review` | SQLAlchemy+Alembic | 修改 Model/迁移 | ✅ |
| ⑥ | `vue` | Vue 3 | 修改 Vue 组件 | ✅ 全部34文件用 `<script setup>`，无 Options API |
| ⑦ | `pinia` | Pinia | 修改 Store | ✅ 2 个 Store 均用 Setup Stores（Composition API） |
| ⑧ | `mysql` | MySQL | 修改表结构/查询 | ✅ 连接池合理、READ-COMMITTED、FK 索引完善 |
| ⑨ | `mysql-best-practices` | MySQL | 修改表结构/查询 | ✅ 参数化查询、pool_pre_ping、pool_recycle |
| ⑩ | `docker-patterns` | Docker | 修改 Dockerfile/compose | ✅ Dockerfile 含健康检查、alembic 自动迁移 |

### 2.3 测试与一致性（提交前）

| # | Skill | 用途 | 触发条件 | 状态 |
|---|-------|------|----------|:---:|
| ⑪ | `webapp-testing` | Playwright 浏览器验证 | 前端功能完成 | ✅ E2E + 跨浏览器测试已就绪 |
| ⑫ | `upkeep` | 文档与代码一致性 | 改配置/表结构/API | ✅ 检测清单已同步更新 |

### 2.4 调试排查（遇到错误时）

| # | Skill | 用途 | 触发条件 | 状态 |
|---|-------|------|----------|:---:|
| ⑬ | `systematic-debugging` | 4阶段铁律调试 | 出现 bug/异常/测试失败 | 🔧 按需使用 |
| ⑭ | `bug-detective` | 快速错误排查（备用） | 简单错误快速定位 | 🔧 按需使用 |

### 2.5 Capacitor 专项检查（二期新增）

> **设计原则**：Capacitor 打包现有 Web = 零额外开发。App 只放"现场高频、需扫码/拍照"的操作，复杂报表/BOM编辑/批量导入导出不放。

#### 2.5.1 架构设计

```
┌─────────────────────────────────────────────┐
│              Android APK                     │
│  ┌───────────────────────────────────────┐  │
│  │         Capacitor WebView              │  │
│  │  ┌─────────────────────────────────┐  │  │
│  │  │    Vue 3 + Element Plus SPA     │  │  │
│  │  │    (Vite 构建 → dist/)           │  │  │
│  │  │                                 │  │  │
│  │  │  axios → /api/v1/*              │  │  │
│  │  └──────────────┬──────────────────┘  │  │
│  └─────────────────┼─────────────────────┘  │
└────────────────────┼────────────────────────┘
                     │ HTTP (cleartext)
                     ▼
         ┌───────────────────┐
         │  FastAPI Backend  │
         │  (port 8000)      │
         └───────────────────┘
```

**方案决策：打包静态资源（方案A）**

| 方案 | 配置 | 优点 | 缺点 | 选择 |
|------|------|------|------|:---:|
| A. 打包静态资源 | `webDir: "dist"`，不配 `server.url` | 离线可用，启动快 | 每次更新需重新打包 | ✅ |
| B. 加载服务器页面 | 配 `server.url: "http://IP:8080"` | 更新即生效，无需重打包 | 必须联网，启动稍慢 | ❌ |

> 理由：内网环境更新不频繁；离线可用，现场信号不好时也能操作；更新时重新发 APK，员工覆盖安装即可。

#### 2.5.2 6 阶段推进计划

| 阶段 | 内容 | 产出 | 依赖 | 状态 |
|:---:|------|------|------|:---:|
| 1 | 环境准备：Android Studio + SDK（API 26+）+ 环境变量 `ANDROID_HOME` | 开发环境就绪 | 开发机 | ⬜ |
| 2 | 生成 Android 项目：`npm run cap:add:android` → `cap:sync` → `cap:open` | `android/` 目录 + 首次构建成功 | 阶段1 | ⬜ |
| 3 | 功能适配：明确 App 功能范围，只放现场高频操作 | 功能清单确认 | 阶段2 | ⬜ |
| 4 | 原生插件集成：preferences / status-bar / camera（3个） | 插件安装完成 | 阶段1 | ⬜ |
| 5 | 打包签名：`./gradlew assembleDebug` → Debug APK | `app-debug.apk` | 阶段2-4 | ⬜ |
| 6 | 分发更新：企业微信群发 / 内网下载页 | 员工可安装使用 | 阶段5 | ⬜ |

#### 2.5.3 App 功能取舍

| 功能 | 是否放 App | 理由 |
|------|:---:|------|
| 扫码查SN | ✅ 必放 | 现场最高频操作 |
| 退货登记 | ✅ 必放 | 现场需要快速录入 |
| 维修确认 | ✅ 必放 | 测试工程师现场用 |
| 审批待办 | ✅ 必放 | 管理层移动审批 |
| 轮询提醒 | ✅ 必放 | 待办通知 |
| 拍照上传 | ✅ 必放 | 维修取证 |
| 复杂报表 | ❌ 不放 | 屏幕小，PC 端更合适 |
| BOM 编辑 | ❌ 不放 | 表格复杂，PC 端操作 |
| 批量导入导出 | ❌ 不放 | 文件操作，PC 端更合适 |
| 用户管理 | ❌ 不放 | 管理员用 PC |

#### 2.5.4 原生插件决策

| 插件 | 建议 | 理由 |
|------|:---:|------|
| `@capacitor/preferences` | ✅ 立即装 | 存 Token，无权限要求，必须 |
| `@capacitor/status-bar` | ✅ 立即装 | 优化显示，无权限要求，简单 |
| `@capacitor/camera` | ✅ 立即装 | 拍照取证，需相机权限但标准 |
| `@capacitor/barcode-scanner` | ⚠️ 暂缓 | 先用 html5-qrcode 跑通，避免改造风险 |
| `@capacitor/network` | ⚠️ 暂缓 | 断网提示是锦上添花，非必需 |

> barcode-scanner 暂缓原因：现有 html5-qrcode 已可用，换原生需改造扫码组件且有兼容性风险。等流程跑通后，根据现场反馈再决定是否切换。

#### 2.5.5 配置与环境检查

| # | 检查项 | 通过标准 | 状态 |
|---|--------|---------|:---:|
| ⑮ | `capacitor.config.json` 配置 | `webDir: "dist"`、`androidScheme: "http"`、`cleartext: true` | ✅ |
| ⑯ | `@capacitor/core` 在 dependencies | `npm ls @capacitor/core` 输出在 dependencies | ✅ |
| ⑰ | `@capacitor/cli` 在 devDependencies | `npm ls @capacitor/cli` 输出在 devDependencies | ✅ |
| ⑱ | `@capacitor/android` 在 dependencies | `npm ls @capacitor/android` 输出在 dependencies | ✅ |
| ⑲ | `.gitignore` 排除 `android/` `ios/` | `git status` 不显示 android/ ios/ | ✅ |
| ⑳ | npm 脚本就绪 | `cap:add:android` / `cap:sync` / `cap:open` 存在 | ✅ |
| ㉑ | 前端构建无错误 | `npm run build` 无报错 | ✅ |
| ㉒ | Android SDK（API 26+） | SDK Manager 中已安装 | ⬜ |
| ㉓ | `ANDROID_HOME` 环境变量 | `echo $ANDROID_HOME` 有输出 | ⬜ |
| ㉔ | 真机调试连接 | `adb devices` 能看到设备 | ⬜ |
| ㉕ | APK 可安装 | Debug APK 可安装在 Android 8.0+ | ⬜ |
| ㉖ | 内网连接 | 安装后启动可连接内网 API | ⬜ |

---

## 三、代码质量检测

### 3.1 重复代码检测（P0）

| # | 检查项 | 说明 | 状态 |
|---|--------|------|:---:|
| 1 | 审计日志函数重复 | 提取 `app/utils/audit.py` 共享 | ✅ |
| 2 | 通用工具抽取 | 响应格式统一 `R.ok()`/`R.fail()` | ✅ |
| **3** | **Station CRUD与Customer CRUD结构对比** | **提取通用CRUD模式（二期）** | ⬜ |
| **4** | **扫码逻辑复用** | **Stocktake扫描与入库扫描复用扫码组件（二期）** | ⬜ |
| **5** | **轮询通知** | **提取为通用composable（usePolling）（二期）** | ⬜ |

### 3.2 代码异味检测（P1）

| # | 检查项 | 说明 | 状态 |
|---|--------|------|:---:|
| 1 | 异常处理 | 资源不存在返回友好错误 | ✅ |
| 2 | 函数过长 | >50行考虑拆分 | ✅ Inbound.vue `buildSavePayload` 77行，其余函数均 <50行 |
| 3 | 魔法数字 | 硬编码状态值→枚举 | ✅ 后端用 `enums.py`，前端用 `constants/enums.js` |
| 4 | 无用导入 | 清理 | ✅ |
| **5** | **盘点锁库存逻辑位置** | **放在Service层而非API层（二期）** | ⬜ |
| **6** | **质保期计算重复** | **提取为`utils/warranty.py`工具函数（二期）** | ⬜ |
| **7** | **DeviceLedger N+1查询** | **使用joinedload或子查询（二期）** | ⬜ |
| **8** | **新枚举值覆盖** | **switch/match覆盖所有枚举值（二期）** | ⬜ |

### 3.3 安全检查

| # | 检查项 | 状态 |
|---|--------|:---:|
| 1 | SQL注入防护（ORM参数化） | ✅ |
| 2 | XSS防护（输出转义） | ✅ |
| 3 | 认证授权（JWT+角色） | ✅ |
| 4 | 密码加密（bcrypt） | ✅ |
| 5 | 敏感信息（不输出密码） | ✅ |
| 6 | CORS配置 | ✅ |
| **7** | **写操作角色权限** | **所有写操作端点使用 AllowWarehouseOrAbove / get_current_admin 而非裸 get_current_user** | ✅ |
| **8** | **盘点调整需审批** | **调整确认操作需记录操作人+审计日志（二期）** | ✅ |
| **9** | **轮询接口不暴露敏感信息** | **仅返回未读数量，不含详情（二期）** | ⬜ |
| **10** | **.env 在 .gitignore 中** | **git status 不显示 .env 文件** | ✅ |
| **11** | **前端代码无硬编码密钥** | **前端代码中无密码/Key/Secret** | ✅ |
| **12** | **.env.example 值留空** | **只含模板值，不含真实密钥** | ✅ |
| **13** | **日志中无密钥泄漏** | **异常日志不输出 JWT_SECRET/DB_PASSWORD** | ✅
| **14** | **后端API角色依赖验证** | **8个API文件写操作 → AllowWarehouseOrAbove(6文件)/get_current_admin(2文件)，读操作保留get_current_user** | ✅ |

### 3.4 性能检测

| # | 检查项 | 状态 |
|---|--------|:---:|
| 1 | N+1查询（`joinedload`） | ✅ |
| 2 | 分页查询 | ✅ |
| 3 | 数据库索引 | ✅ FK 字段均有 `index=True`，业务编号 `unique=True` |
| 4 | 大数据导出 | ✅ 使用 `openpyxl` + `io.BytesIO` 内存生成 |
| **5** | **DeviceLedger按SN查询** | **item_sn已有索引（二期）** | ⬜ |
| **6** | **Station当前设备数统计** | **使用子查询或缓存，避免N+1（二期）** | ⬜ |
| **7** | **盘点大批量SN（1000+）** | **分页加载，单页≤100条（二期）** | ⬜ |
| **8** | **看板统计接口** | **Redis缓存（TTL=60s）（二期）** | ⬜ |
| **9** | **轮询接口轻量化** | **单次查询<50ms（二期）** | ⬜ |
| **10** | **大数据量渲染** | **前端表格1000+行渲染<2s，不卡顿不白屏（二期）** | ⬜ |
| **11** | **慢查询检测** | **复杂筛选（SN+分类+状态+日期）EXPLAIN type非ALL（二期）** | ⬜ |

### 3.5 全局异常处理器检查

| # | 检查项 | 文件 | 通过标准 | 状态 |
|---|--------|------|----------|:---:|
| 1 | 全局异常处理器已注册 | `main.py` L89-L96 | `@app.exception_handler(Exception)` 存在 | ✅ |
| 2 | DEBUG 模式返回 `str(exc)` 详情 | `main.py` L93 | `detail = str(exc) if settings.DEBUG else "服务器内部错误"` | ✅ |
| 3 | DEBUG=False 时返回固定消息 | `main.py` L93 | 返回 `"服务器内部错误"`，不暴露内部细节 | ✅ |
| 4 | 异常已记录到日志 | `main.py` L91 | `logger.exception(...)` 记录完整 Traceback | ✅ |

> **改动量**：仅 `main.py` 1 行差异。不影响 API 格式、数据库结构、现有测试。

### 3.6 参考实现复用检查（P2 — 新增）

| # | 检查项 | 说明 | 状态 |
|---|--------|------|:---:|
| 1 | TablePro 组件复用 | 项目无TablePro组件；6页面统一使用 `el-table stripe v-loading` | ✅ |
| 2 | SearchForm 组件复用 | 项目无SearchForm组件；6页面统一使用 `el-form :inline="true" class="search-bar"` | ✅ |
| 3 | 搜索模式一致 | 各页面按业务需求配置keyword/下拉/状态，均使用inline搜索模式 | ✅ |
| 4 | API 封装一致 | 二期5页面全部通过 `@/api/` 目录封装，无裸`axios`调用 | ✅ |

---

## 四、专项测试补充

### 4.1 E2E 端到端测试（P2 — 上线前最后一步）

| # | 核心流程 | 工具 | 状态 |
|---|---------|------|:---:|
| 1 | 登录→到货→检验→入库 | Playwright | ⚠️ 需启动前端服务 |
| 2 | RMA返修全流程 | Playwright | ⚠️ 需启动前端服务 |
| 3 | 出库→发货 | Playwright | ⚠️ 需启动前端服务 |
| 4 | 冒烟测试(登录页/路由守卫/控制台) | Playwright | ⚠️ 需启动前端服务 |

> **E2E 测试文件**：`backend/tests/e2e/`（`test_smoke.py` 冒烟测试 + `test_e2e_flows.py` 完整流程）
> 冒烟测试无需后端即可运行，验证登录页渲染、路由守卫跳转、控制台零错误。

### 4.2 打印功能专项（P0）

| # | 测试项 | 文件 | 状态 |
|---|--------|------|:---:|
| 1 | `amountToChinese` 金额大写 | `amountToChinese.test.js` | ✅ |
| 2 | `formatAmount` 金额格式化 | `amountToChinese.test.js` | ✅ |
| 3 | `paginateItems` 分页逻辑 | `amountToChinese.test.js` | ✅ |
| 4 | 打印组件渲染 | `print-render.test.js` | ✅ 41 用例 |
| **5** | **打印分页测试** | **25行数据跨页：验证分页数/第2页表头/页码"第2页/共2页"** | ⬜ |

> **已知 Bug**：`amountToChinese` 浮点精度问题（`Math.round(0.99*10)=10`→`CN_NUMS[10]`越界=`undefined`），负数返回空字符串。**v2.3 已修复 ✅**：先归整到分（`Math.round(amount*100)`）消除浮点误差，负数改为抛异常 `throw new Error('金额不能为负数')`。同时修复 `1234.56` 旧测试断言错误（旧代码把5角算成6角）。新增 `formatAmount` 浮点精度验证（0.1+0.2、0.995四舍五入）。

### 4.3 Excel 导入导出专项（P1）

| # | 测试项 | 状态 |
|---|--------|:---:|
| 1 | 导出内容与数据库一致 | ✅ |
| 2 | 长数字文本不被转科学计数法 | ✅ |
| 3 | 物料编码匹配失败友好提示 | ✅ |
| 4 | 多行名称解析 | ✅ |
| 5 | 循环一致性（导出→导入） | ✅ |
| 6 | 边界值与异常 | ✅ |
| **7** | **5000行导出性能** | **导出 < 30s，不含500错误** | ⬜ |

> **测试文件**：`backend/tests/test_excel.py` — 40 用例全部通过 ✅

### 4.4 并发/库存扣减（P1）

| # | 场景 | 工具 | 状态 |
|---|------|------|:---:|
| 1 | 并发确认入库 | `pytest-asyncio`+`asyncio.gather` | ✅ 已补充 |
| 2 | 库存扣减不足 | 单元测试 | ✅ 已补充 |
| 3 | 行级锁并发扣减 | `session.begin()` 模拟 | ✅ 已补充 |
| **4** | **50用户并发** | **asyncio.gather(50) 模拟盘点高峰并发（二期）** | ⬜ |

### 4.5 数据库迁移回滚（P1）

| # | 命令 | 状态 |
|---|------|:---:|
| 1 | 迁移链完整性（根→头可追溯） | ✅ |
| 2 | 所有 down_revision 引用真实存在 | ✅ |
| 3 | 合并迁移（merge）父节点均存在 | ✅ |
| 4 | 每个迁移文件含 upgrade()/downgrade() | ✅ |
| 5 | `alembic downgrade -1`（需要真实数据库） | ✅ 降级→升级双向验证通过 |
| 6 | `alembic upgrade head`（需要真实数据库） | ✅ 已恢复至 head (9fde5cf8e567) |

> **测试文件**：`backend/tests/test_db_migration.py` — 13 用例全部通过 ✅（迁移链静态分析）
> 注：`alembic downgrade/upgrade` 需要真实 MySQL 数据库，在单元测试中无法执行，建议在部署前手动验证。

### 4.6 权限矩阵（P1 — ✅ 已完成 15 用例）

| 角色 | DELETE 用户 | 审计日志 | 系统设置 |
|------|:---:|:---:|:---:|
| ADMIN | ✅ 可访问 | ✅ 可访问 | ✅ 可访问 |
| WAREHOUSE | ✅ 403 | ✅ 403 | ✅ 403 |
| QUALITY | ✅ 403 | ✅ 403 | — |
| PRODUCTION | ✅ 403 | ✅ 403 | ✅ 403 |
| TEST_ENG | ✅ 403 | ✅ 403 | ✅ 403 |

> 已实现：`backend/tests/security/test_permission_matrix.py`（12权限矩阵 + 3越权），全部通过 ✅

### 4.7 浏览器兼容性（P2 — 自动化测试）

| # | 浏览器 | 检查项 | 状态 |
|---|--------|--------|:---:|
| 1 | Chrome | 登录页渲染 / 路由守卫 / 控制台错误 / 移动端视口 | ⚠️ 需启动前端服务 |
| 2 | Edge | 登录页渲染 / 路由守卫 / 控制台错误 / 移动端视口 | ⚠️ 需启动前端服务 |
| 3 | Firefox | 登录页渲染 / 路由守卫 / 控制台错误 | ⚠️ 需启动前端服务 |

> **测试文件**：`backend/tests/e2e/test_cross_browser.py` — 10 用例（Chrome 4 + Edge 4 + Firefox 2 skip）
> 运行：`$env:E2E_BASE_URL="http://localhost:5173"; cd backend; python -m pytest tests/e2e/test_cross_browser.py -v --tb=short`
> **人工测试**：打印预览/分页/样式 仍需在 Chrome/Edge/Firefox 中手动验证。

| # | 浏览器 | 打印兼容检查项 | 状态 |
|---|--------|--------|:---:|
| **4** | **Chrome打印预览** | **6种单据字体/边框/分页一致，@media print正常** | ⬜ |
| **5** | **Edge打印预览** | **6种单据字体/边框/分页一致** | ⬜ |
| **6** | **Firefox打印预览** | **6种单据字体/边框/分页一致** | ⬜ |

### 4.8 环境配置检查（P1/P2）

| # | 检查项 | 状态 |
|---|--------|:---:|
| 1 | .env 文件存在且含必要字段 | ✅ |
| 2 | Python 版本 >= 3.10 | ✅ |
| 3 | 核心依赖安装（fastapi/sqlalchemy/alembic） | ✅ |
| 4 | 目录结构完整性（app/api/models/service/core） | ✅ |
| 5 | 数据库连接字符串解析 | ✅ |
| 6 | Alembic 配置正确 | ✅ |
| 7 | Redis 配置（可选） | ✅ |
| 8 | 前端 Node.js / npm / node_modules | ✅ |
| 9 | 安全配置（JWT/CORS/DEBUG） | ✅ |
| 10 | 日志配置（级别/目录可写） | ✅ |
| **11** | **端口可用性（前端5173 / 后端8000）** | **⬜** |
| **12** | **CORS allow_origin_regex 配置** | **⬜（待批次①执行）** |

> **测试文件**：`backend/tests/test_env_check.py` — 35 用例全部通过 ✅

#### 4.8.1 端口可用性检查（前置步 — v2.12 新增）

> **触发条件**：执行"完整检测清单"时，作为前置步自动执行。
> **脚本路径**：`scripts/check_port.ps1`

| # | 检查项 | 命令 | 通过标准 | 状态 |
|---|--------|------|---------|:---:|
| 1 | 前端端口 5173 未被占用 | `powershell -File scripts/check_port.ps1 -Port 5173` | 输出 `✅ 端口 5173 可用` | ✅ |
| 2 | 后端端口 8000 未被占用 | `powershell -File scripts/check_port.ps1 -Port 8000` | 输出 `✅ 端口 8000 可用` | ✅ |
| 3 | 若端口被占用，输出占用进程信息 | 同上 | 输出进程名 + PID + 释放命令 | ✅ |

**脚本内容**：`scripts/check_port.ps1`

```powershell
param([int]$Port = 5173)

$ErrorActionPreference = "Stop"

$conn = Get-NetTCPConnection -LocalPort $Port -ErrorAction SilentlyContinue
if ($conn) {
    $proc = Get-Process -Id $conn.OwningProcess -ErrorAction SilentlyContinue
    Write-Host "  ❌ 端口 $Port 已被占用" -ForegroundColor Red
    Write-Host "  进程名: $($proc.ProcessName)" -ForegroundColor Yellow
    Write-Host "  PID: $($conn.OwningProcess)" -ForegroundColor Yellow
    Write-Host ""
    Write-Host "  释放端口: taskkill /PID $($conn.OwningProcess) /F" -ForegroundColor Cyan
    exit 1
}
Write-Host "  ✅ 端口 $Port 可用" -ForegroundColor Green
exit 0
```

**背景**：Vite 默认 `strictPort: false`，端口被占时静默漂移到 5174/5175/...，而后端 CORS 通过 `allow_origin_regex` 匹配任意开发端口，确保漂移不影响功能。端口检查脚本用于启动前确认默认端口状态，避免意外漂移。

### 4.9 安卓App专项测试（P0 — 二期新增）

#### 4.9.1 环境检查

| # | 检查项 | 工具 | 通过标准 | 状态 |
|---|--------|------|---------|:---:|
| 1 | Android Studio 安装 | 手动 | 能打开项目 | ⬜ |
| 2 | Android SDK（API 26+） | SDK Manager | 已安装 | ⬜ |
| 3 | `ANDROID_HOME` 环境变量 | `echo $ANDROID_HOME` | 有输出 | ⬜ |
| 4 | 真机调试连接 | `adb devices` | 能看到设备 | ⬜ |

#### 4.9.2 构建检查

| # | 检查项 | 命令 | 通过标准 | 状态 |
|---|--------|------|---------|:---:|
| 1 | 前端构建 | `npm run build` | 无报错 | ✅ |
| 2 | Capacitor 同步 | `npm run cap:sync` | 无报错，dist/ 同步到 android/ | ⬜ |
| 3 | APK 打包 | `./gradlew assembleDebug` | 生成 `app-debug.apk` | ⬜ |
| 4 | APK 大小 | `ls -lh` | < 20MB | ⬜ |
| 5 | APK 安装 | 手动 | 安装成功，图标显示正常 | ⬜ |

#### 4.9.3 功能检查

| # | 测试项 | 工具 | 验收标准 | 状态 |
|---|--------|------|---------|:---:|
| 1 | 启动加载 | 手动 | 3秒内进入登录页 | ⬜ |
| 2 | 登录 | 手动 | 能正常登录，Token 持久化 | ⬜ |
| 3 | 内网连接 | 手动 | 手机连公司 WiFi，能访问后端 | ⬜ |
| 4 | 扫码查SN | 手动 | 扫码后2秒内显示SN详情 | ⬜ |
| 5 | 退货登记 | 手动 | 提交后返回成功，web端可查 | ⬜ |
| 6 | 维修确认 | 手动 | 拍照→上传→提交全流程通 | ⬜ |
| 7 | 审批待办 | 手动 | 能查看和处理待办 | ⬜ |
| 8 | 拍照上传 | 手动 | 能拍照并上传 | ⬜ |
| 9 | 轮询提醒 | 手动 | 30秒后待办数量更新 | ⬜ |
| 10 | 退出登录 | 手动 | 清除 Token，返回登录页 | ⬜ |
| **11** | **网络切换** | **手动** | **WiFi↔4G切换后App不崩溃，请求自动恢复（二期）** | ⬜ |

#### 4.9.4 兼容性检查

| # | 检查项 | 工具 | 通过标准 | 状态 |
|---|--------|------|---------|:---:|
| 1 | Android 8.0 | 手动 | 能正常安装运行 | ⬜ |
| 2 | Android 10 | 手动 | 能正常安装运行 | ⬜ |
| 3 | Android 12+ | 手动 | 权限弹窗正常 | ⬜ |
| 4 | 华为/小米/OPPO/vivo | 手动 | 主流品牌均能运行 | ⬜ |
| 5 | 横屏/竖屏 | 手动 | 竖屏为主，横屏不崩溃 | ⬜ |
| 6 | 不同屏幕尺寸 | 手动 | 布局自适应，不溢出 | ⬜ |

#### 4.9.5 安全检查

| # | 检查项 | 工具 | 通过标准 | 状态 |
|---|--------|------|---------|:---:|
| 1 | 无硬编码密钥 | 代码审查 | 前端代码中无密码/Key | ✅ |
| 2 | Token 存储安全 | 手动 | 用 Capacitor Preferences，不存明文 | ⬜ |
| 3 | HTTP 明文限制 | 手动 | 仅内网 IP 允许 | ⬜ |
| 4 | 相机权限申请 | 手动 | 仅在使用时申请 | ⬜ |

#### 4.9.6 移动端显示比例优化（P0 — 二期新增）

> **方案文档**：[doc/手机端显示比例优化方案.md](doc/手机端显示比例优化方案.md)

| # | 检查项 | 方法 | 通过标准 | 状态 |
|---|--------|------|---------|:---:|
| 1 | 全局移动端 CSS | `src/style.css` | 193行，`@media (max-width: 768px)` 含弹窗/表格/表单/按钮/分页/输入框 | ✅ |
| 2 | viewport 适配 | `index.html` | `min-scale=0.5, max-scale=5.0, viewport-fit=cover, user-scalable=yes` | ✅ |
| 3 | 摄像头权限 | `AndroidManifest.xml` | 含 `android.permission.CAMERA` | ✅ |
| 4 | 双指缩放—HTML层 | `index.html` | viewport 未锁死（`user-scalable=yes, max-scale≥2.0`） | ✅ |
| 5 | 双指缩放—CSS层 | `src/style.css` | `html,body { touch-action: auto }`，无 `touch-action:none` | ✅ |
| 6 | 双指缩放—WebView层 | `MainActivity.java` | `setBuiltInZoomControls(true)`, `setDisplayZoomControls(false)` | ✅ |
| 7 | 弹窗不溢出 | 手机打开任意弹窗 | 弹窗宽度 ≤ 96% 屏幕 | ⬜ 待装机 |
| 8 | 表格可横滑 | 手机打开列表页 | 表格横向可滑动，内容不挤压 | ⬜ 待装机 |
| 9 | 表单上下布局 | 手机打开任意表单 | label 在上、输入框在下 | ⬜ 待装机 |
| 10 | 按钮触摸友好 | 手指点击 | 按钮 ≥ 44×44px | ⬜ 待装机 |
| 11 | 分页不拥挤 | 手机打开列表页 | 分页按钮清晰可点 | ⬜ 待装机 |
| 12 | 输入框不缩放 | iOS 聚焦输入框 | 不触发自动缩放 | ⬜ 待装机 |
| 13 | PC 端无变化 | PC 浏览器 >768px | 样式与之前一致 | ⬜ 待装机 |
| 14 | 扫码可用 | 入库管理→扫码录入 | 调起摄像头，不提示"原生扫码未识别" | ⬜ 待装机 |
| 15 | 双指放大 | 两根手指往外划 | 页面正常放大，无 +/- 按钮 | ⬜ 待装机 |
| 16 | 双指缩小 | 两根手指往里划 | 页面正常缩小，可看全表格 | ⬜ 待装机 |

### 4.10 轮询提醒测试（P0 — 二期新增）

| # | 测试项 | 预期 | 状态 |
|---|--------|------|:---:|
| 1 | 首次加载获取待办数 | 页面显示待办数量 | ✅ |
| 2 | 30秒轮询更新 | 数量变化后30秒内更新 | ✅ |
| 3 | 网络异常降级 | 显示"网络异常"而不崩溃 | ✅ |
| 4 | 页面离开停止轮询 | 切换到其他页面后停止请求 | ✅ |
| 5 | 轮询间隔可配置 | 修改配置后按新频率轮询 | ✅ |

### 4.11 质保期边界测试（P1 — 二期新增）

| # | 场景 | 预期 | 状态 |
|---|------|------|:---:|
| 1 | warranty_start = warranty_end（安装即到期） | in_warranty = true（含当天） | ✅ test_phase2_unit.py |
| 2 | warranty_end = 昨天 | in_warranty = false | ✅ test_phase2_unit.py |
| 3 | warranty_end = 明天 | in_warranty = true | ✅ test_phase2_unit.py |
| 4 | warranty_start = NULL, warranty_end = 有值 | 按 end 判断 | ✅ test_phase2_unit.py |
| 5 | warranty_start = 有值, warranty_end = NULL | 视为永久在保 | ✅ test_phase2_unit.py |
| 6 | 全部为 NULL | 视为无质保信息 | ✅ test_phase2_unit.py |

### 4.12 盘点并发测试（P1 — 二期新增）

| # | 场景 | 预期 | 状态 |
|---|------|------|:---:|
| 1 | 两个用户同时扫码同一SN | 后到者提示"已扫码" | ✅ test_phase2_concurrency.py |
| 2 | 全面盘点中尝试创建出库单 | 409 拒绝 | ✅ test_phase2_concurrency.py |
| 3 | 同时创建两个全面盘点 | 第二个提示"已有盘点进行中" | ✅ test_phase2_concurrency.py |
| 4 | 盘点完成中同时确认调整 | 调整确认串行化 | ✅ test_phase2_concurrency.py |

### 4.13 数据一致性测试（P1 — 二期新增）

| # | 检查项 | 验证逻辑 | 状态 |
|---|--------|---------|:---:|
| 1 | DeviceLedger ↔ InventoryItem | 所有removed_date IS NULL的SN，current_location匹配Station | ✅ test_phase2_consistency.py |
| 2 | 盘点后库存调整一致性 | 调整确认后InventoryItem状态与Adjustment一致 | ✅ test_phase2_consistency.py |
| 3 | StocktakeLine差异计算 | diff_qty = actual_qty - system_qty | ✅ test_phase2_consistency.py |
| 4 | 质保期JSON序列化 | warranty_start/end日期格式一致性 | ✅ test_phase2_consistency.py |

### 4.14 Capacitor 打包验证（P0 — 二期新增）

#### 阶段1：环境准备

| # | 检查项 | 命令/工具 | 通过标准 | 状态 |
|---|--------|---------|---------|:---:|
| 1 | Android Studio 安装 | 手动 | 能正常打开 | ⬜ |
| 2 | Android SDK（API 26+） | SDK Manager | Platforms 26+ 已安装 | ⬜ |
| 3 | `ANDROID_HOME` | `echo $ANDROID_HOME` | 输出路径 | ⬜ |
| 4 | Gradle | `gradle --version` | >= 8.0 | ⬜ |

#### 阶段2：生成 Android 项目

| # | 检查项 | 命令 | 通过标准 | 状态 |
|---|--------|------|---------|:---:|
| 1 | 添加 Android 平台 | `npm run cap:add:android` | `android/` 目录生成 | ⬜ |
| 2 | 构建前端 | `npm run build` | `dist/` 生成无报错 | ✅ |
| 3 | 同步到 Android | `npm run cap:sync` | `dist/` 内容复制到 `android/app/src/main/assets/` | ⬜ |
| 4 | Gradle 同步 | Android Studio → Sync | 依赖解析无报错 | ⬜ |
| 5 | 首次构建 | `./gradlew assembleDebug` | BUILD SUCCESSFUL | ⬜ |

#### 阶段3：功能适配

| # | 检查项 | 通过标准 | 状态 |
|---|--------|---------|:---:|
| 1 | 功能清单确认 | 已列出 App 包含/不包含的功能 | ✅ 已列出（见 §2.5.3） |
| 2 | 扫码查SN | 扫码后正确显示设备信息 | ⬜ |
| 3 | 退货登记 | 提交成功后 web 端可查 | ⬜ |
| 4 | 维修确认 | 拍照→上传→提交通过 | ⬜ |
| 5 | 审批待办 | 列表显示正确，可处理 | ⬜ |
| 6 | 轮询提醒 | 30秒内待办数更新 | ⬜ |

#### 阶段4：原生插件集成

| # | 检查项 | 命令/工具 | 通过标准 | 状态 |
|---|--------|---------|---------|:---:|
| 1 | 安装 preferences | `npm install @capacitor/preferences` | 安装成功 | ⬜ |
| 2 | 安装 status-bar | `npm install @capacitor/status-bar` | 安装成功 | ⬜ |
| 3 | 安装 camera | `npm install @capacitor/camera` | 安装成功 | ⬜ |
| 4 | AndroidManifest 权限 | `grep CAMERA android/app/src/main/AndroidManifest.xml` | 含相机权限声明 | ⬜ |
| 5 | 插件同步 | `npx cap sync` | 无 MissingPlugin 错误 | ⬜ |

#### 阶段5：打包与产物

| # | 检查项 | 命令 | 通过标准 | 状态 |
|---|--------|------|---------|:---:|
| 1 | 前端构建 | `npm run build` | 无报错 | ✅ |
| 2 | Capacitor 同步 | `npx cap sync android` | 无报错 | ⬜ |
| 3 | Debug APK | `./gradlew assembleDebug` | BUILD SUCCESSFUL | ⬜ |
| 4 | 产物路径 | `ls android/app/build/outputs/apk/debug/` | `app-debug.apk` 存在 | ⬜ |
| 5 | APK 大小 | `ls -lh` | < 20MB | ⬜ |

#### 阶段6：分发与更新

| # | 检查项 | 通过标准 | 状态 |
|---|--------|---------|:---:|
| 1 | 企业微信群分发 | 员工能下载安装 | ⬜ |
| 2 | 内网下载页 | `http://服务器IP:8080/download/ims.apk` 可访问 | ⬜ |
| 3 | 覆盖安装 | 新版本覆盖旧版本，数据不丢失 | ⬜ |
| 4 | 版本号检查 | App 启动时检查版本号 | ⬜ |

#### 风险与应对

| # | 风险 | 概率 | 应对 |
|---|------|:---:|------|
| 1 | Android Studio 环境配置失败 | 中 | 提前准备，找有 Android 经验的同事协助 |
| 2 | 不同品牌手机兼容性问题 | 中 | 提前确认员工手机型号，优先测主流品牌 |
| 3 | 扫码插件兼容性 | 低 | 先用 html5-qrcode 兜底，原生插件作增强 |
| 4 | 内网 IP 变化导致 App 无法连接 | 低 | 服务器设静态 IP，或提供配置入口 |
| 5 | APK 体积过大 | 低 | Capacitor 打包通常 < 10MB |

### 4.15 前端路由遍历测试（P0 — 500 排查专项）

> **目标**：admin 登录后遍历全部 27 个页面，监听所有网络请求，检查是否出现 500。
> **参考方案**：[doc/500错误排查与测试补充方案.md](../../doc/500错误排查与测试补充方案.md) 第5章

| # | 文件 | 检查项 |
|---|------|--------|
| 1 | `tests/e2e/test_all_pages_500.py` | admin 登录 → 遍历页面 → 监听 response status ≥ 500 |
| 2 | 一期页面（~20个） | dashboard/inventory/inbound/incoming/rma/shipment/bom/production-task/outbound/snapshot/products/partners/workflow/settings/about/docs 等 |
| 3 | 二期页面（5个） | station/device-ledger/stocktake/adjustment/customers |
| 4 | 空数据渲染 | 各页面无数据时不白屏、不崩溃、显示empty-text |
| 5 | 加载状态 | 各页面请求中有loading遮罩，防重复提交 |
| 6 | 404路由处理 | 访问 /nonexistent 跳转404页或首页，非白屏 |
| 7 | 请求超时处理 | 模拟慢响应（F12→Slow 3G），验证超时提示/重试按钮 |

> **运行命令**：
> ```bash
> $env:E2E_BASE_URL="http://localhost:5173"; cd backend; python -m pytest tests/e2e/test_all_pages_500.py -v --tb=short
> ```

### 4.16 多角色遍历测试（P2）

> **目标**：5 种角色 × 27 页面，验证不同角色访问时权限隔离正确、无 500。
> **测试文件**：`tests/e2e/test_multi_role_traversal.py`（独立 Playwright 脚本，需前端服务运行）
> **角色**：管理员 / 仓库管理员 / 质量负责人 / 生产负责人 / 测试工程师
>
> **运行命令**：
> ```bash
> $env:E2E_BASE_URL="http://localhost:5173"; cd backend; uv run python tests/e2e/test_multi_role_traversal.py
> ```
>
> **注意**：无权限页面会跳转 `/dashboard`，也算通过（非 500）。

### 4.17 APK 打包检查（P0 — 二期新增）

> **方案来源**：[doc/APK打包检查方案.md](doc/APK打包检查方案.md)
> **打包脚本**：`scripts/build_apk.ps1`（9 步自动化）
> **目标**：打包前强制检查代码状态，防止"代码未提交就打包"。

#### 4.17.1 打包前检查（build_apk.ps1 步骤 ①~②）

| # | 检查项 | 命令 | 通过标准 | 状态 |
|---|--------|------|---------|:---:|
| 1 | 工作区干净 | `git status --porcelain` | 无输出（无未提交变更） | ⬜ |
| 2 | 关键文件 (1/6) `apiConfig.js` | `Test-Path frontend/src/utils/apiConfig.js` | 文件存在 | ⬜ |
| 3 | 关键文件 (2/6) `ServerSettingsDialog.vue` | `Test-Path frontend/src/components/ServerSettingsDialog.vue` | 文件存在 | ⬜ |
| 4 | 关键文件 (3/6) `request.js` | `Test-Path frontend/src/utils/request.js` | 文件存在 | ⬜ |
| 5 | 关键文件 (4/6) `Login.vue` | `Test-Path frontend/src/views/Login.vue` | 文件存在 | ⬜ |
| 6 | 关键文件 (5/6) `settings.py` | `Test-Path backend/app/api/settings.py` | 文件存在 | ⬜ |
| 7 | 关键文件 (6/6) `AndroidManifest.xml` | `Test-Path frontend/android/app/src/main/AndroidManifest.xml` | 文件存在 | ⬜ |

#### 4.17.2 构建前检查（build_apk.ps1 步骤 ③~⑥）

| # | 检查项 | 命令 | 通过标准 | 状态 |
|---|--------|------|---------|:---:|
| 1 | 清理 dist/ | `Remove-Item -Recurse -Force frontend/dist/` | dist/ 为空 | ⬜ |
| 2 | Node 可用 | `node --version` | 输出版本号 | ⬜ |
| 3 | 后端测试通过 | `cd backend && uv run pytest tests/ -v --tb=short` | 全部通过 | ⬜ |
| 4 | 前端测试通过 | `cd frontend && npm run test` | 全部通过 | ⬜ |
| 5 | 前端构建成功 | `cd frontend && npm run build` | 无报错，dist/index.html 存在 | ⬜ |
| 6 | dist 非空 | `Test-Path frontend/dist/index.html` | 文件存在 | ⬜ |

#### 4.17.3 打包与产物（build_apk.ps1 步骤 ⑦~⑨）

| # | 检查项 | 命令 | 通过标准 | 状态 |
|---|--------|------|---------|:---:|
| 1 | Capacitor 同步 | `cd frontend && npx cap sync android` | 无报错，dist/ 同步到 android/ | ⬜ |
| 2 | Gradle 打包 | `cd frontend/android && ./gradlew assembleDebug` | BUILD SUCCESSFUL | ⬜ |
| 3 | APK 产物存在 | `Test-Path frontend/android/app/build/outputs/apk/debug/app-debug.apk` | 文件存在 | ⬜ |
| 4 | APK 大小 | `ls -lh` | < 20MB | ⬜ |
| 5 | APK 复制到 dist-apk/ | `Copy-Item ... dist-apk/DL-IMS-{version}-{date}.apk` | 文件存在 | ⬜ |
| 6 | 版本号注入 | `vite.config.js` 含 `__APP_VERSION__` + `__BUILD_TIME__` | 构建时注入 | ⬜ |
| 7 | 关于页面可用 | 启动 App → 用户下拉 → 关于 | 显示版本号/构建时间/服务器地址 | ⬜ |

#### 4.17.4 打包脚本一键运行

| # | 检查项 | 命令 | 通过标准 | 状态 |
|---|--------|------|---------|:---:|
| 1 | 脚本文件存在 | `Test-Path scripts/build_apk.ps1` | 文件存在 | ⬜ |
| 2 | 一键打包 | `powershell -File scripts/build_apk.ps1` | 9 步全部通过 | ⬜ |

**脚本输出示例：**
```
========================================
DL-IMS APK Build Script
========================================
[1/9] 检查工作区状态... ✅ 干净
[2/9] 检查关键文件... ✅ 6/6
[3/9] 清理 dist/... ✅
[4/9] 后端测试... ✅ 通过
[5/9] 前端测试... ✅ 通过
[6/9] 前端构建... ✅ dist/ 生成
[7/9] Capacitor 同步... ✅
[8/9] Gradle 打包... ✅
[9/9] 复制 APK... ✅

========================================
打包完成！
  版  本：2.0.0
  构建时间：2026-09-15 14:30:00
  最新提交：abc1234 [app] 新增打包脚本和关于页面
  APK 路径：dist-apk/DL-IMS-2.0.0-20260915-143000.apk
  下载路径：backend/static/download/ims-latest.apk
========================================
```

#### 4.17.5 APK 下载功能验证（新增 — 2026-09-16）

| # | 检查项 | 命令/操作 | 通过标准 | 状态 |
|---|--------|----------|---------|:---:|
| 1 | `/download/ims-latest.apk` 可访问 | 启动后端 → 浏览器访问 `http://IP:8000/download/ims-latest.apk` | 触发文件下载，非404 | ⬜ |
| 2 | About 页面下载按钮正常 | 登录 → 右上角用户下拉 → 关于 → 点击"下载 Android App" | 按钮显示，点击触发下载 | ⬜ |
| 3 | 登录页下载链接可点击 | 退出登录 → 登录页底部"下载移动端 App" | 链接显示，点击触发下载 | ⬜ |
| 4 | 下载链接 IP 动态获取 | 修改服务器地址后刷新 About 页 | 下载链接跟随 API 地址变化 | ⬜ |
| 5 | 构建脚本自动输出固定文件名 | `powershell -File scripts/build_apk.ps1` 后检查 `backend/static/download/ims-latest.apk` | 文件存在，名为 `ims-latest.apk` | ⬜ |

#### 4.17.6 APK 远程加载功能验证（新增 — 2026-09-16）

> **方案来源**：[doc/APK下载与远程加载方案.md](doc/APK下载与远程加载方案.md)

| # | 检查项 | 命令/操作 | 通过标准 | 状态 |
|---|--------|----------|---------|:---:|
| 1 | `/health` 端点可访问 | `curl http://IP:8000/health` | 返回 200，含 `status` 字段 | ⬜ |
| 2 | `VITE_FRONTEND_PORT` 已配置 | 检查 `.env.production` | 包含 `VITE_FRONTEND_PORT=8080` | ⬜ |
| 3 | `getFrontendUrl()` 端口推导正确 | 浏览器 Console 测试 `getFrontendUrl('http://IP:8000')` | 返回 `http://IP:8080`（非字符串替换） | ⬜ |
| 4 | **首次配置引导**（App环境） | 清除 App 数据 → 启动 → 无服务器地址 | 页面显示"首次使用"引导 + 配置按钮 | ⬜ |
| 5 | **"使用在线版本"按钮出现** | 配置服务器后 → 刷新登录页 | 按钮显示"使用在线版本（最新功能）" | ⬜ |
| 6 | **连通性测试 → 跳转远程 → Token传递** | 已登录 App → 点击"使用在线版本" | 跳转到 `http://IP:8080/login`，Token 自动恢复，无需重新登录 | ⬜ |
| 7 | **跳转 Loading 过渡** | 点击"使用在线版本" → 等待 | 显示遮罩层"正在连接服务器..." | ⬜ |
| 8 | **连通性测试失败 → 错误提示** | 关闭后端 → 点击"使用在线版本" | 弹窗提示"服务器不可达" | ⬜ |
| 9 | **记住选择后自动跳转** | 在线上版中使用后 → 重启 App | 自动检测连通 → 跳转远程 | ⬜ |
| 10 | **服务器不可达时静默停留** | 关闭后端 → 重启 App（已选在线） | 停留在本地登录页，无报错 | ⬜ |
| 11 | **"切回本地版本"按钮** | 在线版 → 关于页 | 显示"切回本地版本"按钮 | ⬜ |
| 12 | **切回本地 → 重启回本地** | 点击"切回本地版本" → 确认 → 重启 App | 停留在本地页面，不再跳转 | ⬜ |
| 13 | **关于页运行模式标签** | App内打开关于页 | 在线版显示绿色"在线版"标签，本地版显示灰色"本地版" | ⬜ |

---

### 4.18 UI 视觉一致性检查（P2 — 新增）

| # | 检查项 | 说明 | 状态 |
|---|--------|------|:---:|
| 1 | 颜色一致 | 全部使用 Element Plus 标准色系（primary/danger/warning/success/info），差异值颜色 `#67c23a`/`#f56c6c` 与el-color一致 | ✅ |
| 2 | 字体一致 | 无自定义 font-family；提示文字统一 13px；标签默认 14px | ✅ |
| 3 | 间距一致 | `.search-bar{margin-bottom:4px}` / `.toolbar{margin-bottom:12px}` / `.list-pagination{margin-top:16px}` — 6页面全部统一 | ✅ |
| 4 | 按钮风格一致 | `el-button type="primary"`(主操作) / `type="danger" link`(删除) / `type="warning" link`(回收) — 完全一致 | ✅ |
| 5 | 二期5页面专项 | Station / DeviceLedger / Stocktake / InventoryAdjustment / Customer 逐一检查通过 | ✅ |

### 4.19 Git 规范检查（P0 — 新增）

| # | 检查项 | 命令 | 通过标准 | 状态 |
|---|--------|------|---------|:---:|
| 1 | commit message 格式 | `git log --oneline -5` | 每条符合 `[模块名] 操作说明` 格式 | ✅ 20条全部符合 |
| **2** | **里程碑 Tag** | **`git tag -n`** | **每条主线验收后有对应 Tag（当前仅3个：v1.0/v2.0/v2.0-phase2，缺主线A/B/C/D Tag）** | **⚠️ 不完整** |

---

## 五、上线检查清单

### 5.1 数据库

| # | 检查项 | 命令 | 状态 |
|---|--------|------|:---:|
| 1 | 迁移脚本 | `alembic upgrade head` | ✅ |
| 2 | 种子数据 | `python tests/seed_data.py` | ✅ |
| 3 | 测试账号清理 | `DELETE FROM sys_user WHERE username IN (...)` | ✅ |
| **4** | **数据备份恢复演练** | **`docker exec ims-db mysqldump ... > backup.sql`，验证文件>0KB且能还原** | ⬜ |

### 5.2 环境配置

| # | 检查项 | 状态 |
|---|--------|:---:|
| 1 | `.env` 配置（DB/JWT/Redis） | ✅ |
| 2 | 数据库连接 | ✅ |
| 3 | 测试账号已清理 | ✅ |
| **4** | **二期迁移脚本（新增5表）** | **`alembic upgrade head`（二期）** | ⬜ |
| **5** | **迁移回滚验证** | **`alembic downgrade -1` × N次（二期）** | ⬜ |
| **6** | **现有数据未受影响** | **`SELECT COUNT(*) FROM customer`（二期）** | ⬜ |

### 5.3 二期功能验证（新增）

| # | 检查项 | 验证方式 | 状态 |
|---|--------|---------|:---:|
| 1 | Station CRUD正常 | Web端创建/编辑/删除场站 | ⚠️ 需启动前端 |
| 2 | DeviceLedger自动创建 | 出库→自动创建台账 | ⚠️ 需启动前端 |
| 3 | 质保期查询 | RMA退货录入SN→显示质保状态 | ⚠️ 需启动前端 |
| 4 | 循环盘点 | 创建→扫码→完成→差异报告 | ⚠️ 需启动前端 |
| 5 | 全面盘点+锁定 | 创建→锁定验证→扫码→完成→解锁 | ⚠️ 需启动前端 |
| 6 | 盘点调整确认 | 确认调整→库存状态变更 | ⚠️ 需启动前端 |
| 7 | 数据看板 | 首页显示今日概览 | ⚠️ 需启动前端 |
| 8 | App登录 | 安装APK→输入内网IP→登录 | ❌ 未搭建 |
| 9 | App扫码 | 扫码→显示SN详情 | ❌ 未搭建 |
| 10 | 轮询提醒 | 待办数30秒内自动更新 | ❌ 未实现 |

### 5.4 性能验证（新增）

| # | 检查项 | 指标 | 状态 |
|---|--------|------|:---:|
| 1 | 看板接口响应 | < 500ms | ⚠️ 需真实环境 |
| 2 | 盘点扫码响应 | < 1s（含SN查询） | ⚠️ 需真实环境 |
| 3 | 1000条SN盘点列表 | < 2s（分页首屏） | ⚠️ 需真实环境 |
| 4 | 轮询接口响应 | < 50ms | ❌ 未实现 |
| **5** | **系统启动时间** | **< 10s（docker compose up -d → 健康检查通过）** | ⬜ |

---

### 5.5 需求验收对照（P0 — 新增）

> **目标**：对照 PRD.md 逐项验收功能是否实现，而非只跑测试。

| # | 检查项 | 验收方式 | 状态 |
|---|--------|---------|:---:|
| 1 | 主线A（采购来料）：到货→检验→入库→退货 全流程 | IncomingReceipt+Inbound API ✅ 来料检验/入库/退货端点完整 | ✅ |
| 2 | 主线B（返厂维修）：退货→诊断→维修→质检→再出货/报废 | RMA API ✅ 退货/维修/诊断/SN换新端点完整 | ✅ |
| 3 | 主线C（出货管理）：出货单+SN列表+物流+U9关联 | Shipment+Outbound API ✅ 出货/物流/U9关联端点完整 | ✅ |
| 4 | 主线D（BOM生产准备）：BOM导入/导出+齐套分析+采购建议 | BOM+ProductionTask API ✅ BOM/齐套/任务端点完整 | ✅ |
| 5 | 二期（场站+设备台账+盘点+调整+客户） | 5模块API+Vue页面全部完成 ✅ 契约测试43用例通过 | ✅ |
| 6 | 权限矩阵（4角色×各模块） | 8API文件写操作=AllowWarehouseOrAbove/get_current_admin；读操作=get_current_user ✅ | ✅ |

---

## 六、数据字典更新

> 对应宪法规则31：每次表结构变更后必须运行。此步骤确保 `docs/附录/数据字典.md` 与最新 Model 定义一致。

### 6.1 数据字典生成

| # | 检查项 | 命令 | 状态 |
|---|--------|------|:---:|
| 1 | 运行数据字典生成脚本 | `cd ims-main/backend && uv run python scripts/generate_data_dict.py` | ✅ |
| 2 | 验证输出文件已更新 | 检查 `docs/附录/数据字典.md` 时间戳 | ✅ |
| 3 | 确认无未归类表 | 输出中无"待归类表清单"或已手动补充 `TABLE_OVERRIDE` | ✅ 39张表全部归类 |
| **4** | **二期新表覆盖** | **Station/DeviceLedger/Stocktake/StocktakeLine/InventoryAdjustment（二期）** | ✅ 已含 |
| **5** | **修改表已更新** | **Customer表含8个新字段（二期）** | ✅ 已含 |
| **6** | **总表数更新** | **39表 → 44表（二期）** | ✅ 44表527字段 |

**脚本说明**：扫描 `backend/app/models/*.py` 下所有 SQLAlchemy 模型，按 `FILE_MODULE_MAP` 自动分组（主线A/B/C/D、库存管理、基础数据、支撑模块），提取字段名、类型、必填、默认值、注释，生成 Markdown 格式的数据字典。

**运行命令：**
```bash
cd ims-main/backend && uv run python scripts/generate_data_dict.py
```

---

## 七、状态查看

当前执行结果不在本文件维护。请查看根目录 `PROJECT_STATE.md` 获取最近一次检测的真实结果和已知问题。

运行命令（在 `ims-main/backend/` 目录下执行）：

```bash
uv run pytest tests/smoke/test_api_smoke_all.py -q --tb=short          # API冒烟
uv run pytest tests/contract/test_settings_contract.py -q --tb=short    # 设置契约
uv run python tests/e2e/test_multi_role_traversal.py                    # 多角色遍历（需前端运行）
```

## v2.14 变更记录（2026-09-18 — API角色限制）

| # | 变更内容 |
|---|---------|
| 1 | 版本号 v2.13 → v2.14 |
| 2 | 新增 3.3 #7：写操作角色权限检查（所有写操作端点使用 AllowWarehouseOrAbove / get_current_admin） |
| 3 | 新增 3.3 #14：后端API角色依赖验证详细检查（8文件×写操作全部覆盖） |
| 4 | 标记 3.3 #7（Station/DeviceLedger权限）为 ✅ → 已为8个API文件添加角色依赖 |
| 5 | 标记 3.3 #8（盘点调整审批）为 ✅ → inventory_adjustment.py 写操作使用 AllowWarehouseOrAbove |
| 6 | 更新 密钥管理行：6→7项，4→6✅，通过率 66%→86% |
| 7 | 更新 执行顺序：优先检查角色依赖（服务端最后一道防线） |
| 8 | 头行状态新增：API角色依赖 ✅ 8/8 |

## v2.13 变更记录（2026-09-16 / 09-17 P0更新）

| # | 变更内容 |
|---|---------|
| 1 | 版本号 v2.12 → v2.13 |
| 2 | 新增 4.17.6：APK 远程加载功能验证（13项：健康检查 + 环境变量 + 端口推导 + 首次配置引导 + 在线切换 + Token传递 + Loading + 连通失败 + 自动跳转 + 静默停留 + 切回本地 + 重启验证 + 运行模式标签） |
| 3 | 更新 APK打包检查 汇总数：27 → 40 |
| 4 | 关联文档：`doc/APK下载与远程加载方案.md` |
| 5 | **P0-09/17** 3.5 全局异常处理器 #2~#3 ✅ 已实现 |
| 6 | **P0-09/17** 1.3 边界测试 #10 序列化异常 ✅ 8/8 |
| 7 | **P0-09/17** 6.1 数据字典 #4~#6 ✅ 44表527字段 |
| 8 | **P0-09/17** 3.3 密钥管理 #10~#13 ✅ 4/4 |
| 9 | **P0-09/17** 4.19 Git规范 #1 ✅ commit格式 20条，#2 ⚠️ 缺主线Tag |
| 10 | **P0-09/17** 七 总结更新：边界39全✅ / 7项新完成 |

## v2.12 变更记录（2026-09-16）

| # | 变更内容 |
|---|---------|
| 1 | 版本号 v2.11 → v2.12 |
| 2 | 新增 4.8.1：端口可用性检查（check_port.ps1，前置步） |
| 3 | 新增 4.8 #11-12：端口可用性 + CORS allow_origin_regex 环境检查 |
| 4 | 新增 未完成项 #30-31：端口检查脚本 + CORS allow_origin_regex 改造 |
| 5 | 执行顺序新增"前置步 → 端口可用性检查" |

## v2.11 变更记录（2026-09-16）
| 6 | 新增 4.7.4-6：浏览器打印兼容（Chrome/Edge/Firefox 打印预览） |
| 7 | 新增 4.15.6-7：404路由处理 + 请求超时处理 |
| 8 | 新增 4.18：UI视觉一致性检查（5项） |
| 9 | 新增 4.19：Git规范检查（2项：commit格式 + 里程碑Tag） |
| 10 | 新增 5.1.4：数据备份恢复演练 |
| 11 | 新增 5.4.5：系统启动时间 < 10s |
| 12 | 新增 5.5：需求验收对照（6项：4主线+二期+权限矩阵） |
| 13 | 更新 七：总结表 + 未完成项新增 10 项（#20~#29） |
| 14 | 来源：检测清单评审（讨论过的6项 + 新发现4项 = 最终加入10项） |

## v2.10 变更记录（2026-09-16）

| # | 变更内容 |
|---|---------|
| 1 | 版本号 v2.9 → v2.10 |
| 2 | 新增 1.3.10：序列化异常测试（datetime/Decimal/None 不可序列化字段） |
| 3 | 新增 3.4.10-11：大数据量渲染 + 慢查询检测 |
| 4 | 新增 4.4.4：50用户并发测试（asyncio.gather(50)） |
| 5 | 新增 4.9.3.11：App 网络切换测试（WiFi↔4G） |
| 6 | 新增 4.15.4-5：空数据渲染 + 加载状态检查（前端遍历子项） |
| 7 | 更新 七：未完成项新增 7 项（#13~#19） |
| 8 | 来源：`doc/检测项目遗漏分析.md` 评审通过（P0×1 / P1×3 / P2×2 / P3×1） |

## v2.9 变更记录（2026-09-15）

| # | 变更内容 |
|---|---------|
| 1 | 版本号 v2.8 → v2.9 |
| 2 | 新增 4.17：APK 打包检查（22 项：打包前检查7项 + 构建前检查6项 + 打包产物7项 + 一键运行2项） |
| 3 | 更新执行顺序：新增"第5步 → APK 打包检查"，原步骤顺序后移 |
| 4 | 更新 七：总结表新增"APK打包检查"一行（22项），未完成项新增第12项 |
| 5 | 更新 `.trae/rules/project_rules.md` 规则17：5步 → 7步，新增步骤⑤ APK 打包检查 + 步骤⑦ 数据字典更新 |
| 6 | 关联文档：`doc/APK打包检查方案.md` |

## v2.7 变更记录（2026-09-14）

| # | 变更内容 |
|---|---------|
| 1 | 版本号 v2.6 → v2.7 |
| 2 | 更新 1.1：5项二期契约测试 ⬜ → ✅（43用例全部通过） |
| 3 | 更新 1.2：4项二期场景测试 ⬜ → ✅（S007-S010全部通过） |
| 4 | 更新 1.3：4项二期边界测试 ⬜ → ✅ |
| 5 | 更新 1.4：5项二期前端单元测试 ⬜ → ❌ 未创建 |
| 6 | 更新 1.5：4项二期单元专项 ⬜ → ✅（18用例通过） |
| 7 | 更新 4.11-4.13：质保期边界/盘点并发/数据一致性 ⬜ → ✅ |
| 8 | 更新 五：二期功能验证 ⬜ → ⚠️ 需启动前端 |
| 9 | 更新 六：数据字典 ⬜ → ⚠️ 需运行脚本 |
| 10 | 重写 七：总结基于实际测试结果（后端346+73=419用例，前端124用例） |
| 11 | 删除 二期待完成 ~167项 的计划数字，全部替换为实际状态 |

## v2.8 变更记录（2026-09-15）

| # | 变更内容 |
|---|---------|
| 1 | 版本号 v2.7 → v2.8 |
| 2 | 更新执行顺序：标注 API 全量冒烟 / 前端路由遍历 / 多角色遍历 / 全局异常处理器 |
| 3 | 新增 1.6：后端 API 全量冒烟测试（A/B/C 三类 + 汇总，~94 端点，覆盖 GET/POST/PUT/DELETE） |
| 4 | 新增 3.5：全局异常处理器检查（4 项：已注册/DEBUG详情/生产隐藏/日志记录） |
| 5 | 新增 4.15：前端路由遍历测试（P0，admin 登录后遍历 23 个页面，监听 500） |
| 6 | 新增 4.16：多角色遍历测试（P2，5 角色 × 23 页面） |
| 7 | 更新 七：总结表新增 5 行（API冒烟/前端遍历/多角色遍历/全局异常处理器）+ 未完成项新增 4 项 |
| 8 | 关联文档：`doc/500错误排查与测试补充方案.md` |
### 4.20 质保提醒配置测试（P1 — v2.17 新增）

> **目标**：验证质保到期提醒配置界面和后端统计联动，确保配置可持久化、即时生效。
> **测试文件**：`tests/contract/test_warranty_alert_contract.py`（21 用例，已创建，当前全部 skip — API 未实现）
> **关联改善点**：改善点 #1（质保到期主动提醒）— 可配置周期、可开关

#### 4.20.1 配置接口契约测试

| # | 场景 | 请求 | 预期 | 状态 |
|---|------|------|------|:---:|
| 1 | 获取默认配置（无 sys_config 记录） | `GET /settings/warranty-alert` | 返回 `{"enabled":true,"days":[30,60,90]}` | ⬜ |
| 2 | 管理员更新配置 | `PUT {"enabled":true,"days":[15,45,120]}` | 返回 200，`days` 升序为 `[15,45,120]` | ⬜ |
| 3 | 获取更新后配置 | `GET /settings/warranty-alert` | 返回更新后的值 | ⬜ |
| 4 | 管理员关闭提醒 | `PUT {"enabled":false,"days":[30,60,90]}` | 返回 200，`enabled=false`，`days` 保留 | ⬜ |
| 5 | 重新开启提醒 | `PUT {"enabled":true,"days":[30,60,90]}` | 返回 200，恢复原配置 | ⬜ |
| 6 | 普通用户（WAREHOUSE）修改 | `PUT` with warehouse token | 返回 403 | ⬜ |
| 7 | 未登录修改 | `PUT` without token | 返回 401 | ⬜ |
| 8 | 非法 days（空数组） | `PUT {"enabled":true,"days":[]}` | 返回 422 | ⬜ |
| 9 | 非法 days（含 0） | `PUT {"enabled":true,"days":[0,30]}` | 返回 422 | ⬜ |
| 10 | 非法 days（含 366） | `PUT {"enabled":true,"days":[30,366]}` | 返回 422 | ⬜ |
| 11 | 重复天数自动去重 | `PUT {"enabled":true,"days":[30,30,60]}` | 返回 200，保存为 `[30,60]` | ⬜ |
| 12 | 乱序自动升序 | `PUT {"enabled":true,"days":[90,30,60]}` | 返回 200，保存为 `[30,60,90]` | ⬜ |

#### 4.20.2 仪表盘质保统计联动测试

| # | 场景 | 预期 | 状态 |
|---|------|------|:---:|
| 13 | 开启提醒时 phase2-stats 含 warranty 数据 | `warranty_alert_enabled=true`, `warranty_expiring_by_day` 含分档数据 | ⬜ |
| 14 | 关闭提醒时 warranty_expiring_soon=0 | `enabled=false` 后所有质保统计字段为 0 | ⬜ |
| 15 | poll-status 跟随配置 | `warranty_expiring_soon` 值随配置开关变化 | ⬜ |
| 16 | 修改配置后立即生效 | PUT 修改后下一次 GET 即反映新配置 | ⬜ |
| 17 | 设备无 warranty_end | 不计入任何统计档位 | ⬜ |
| 18 | warranty_end = 今天 | 计入最小档位（如 30 天档） | ⬜ |
| 19 | 累计统计正确 | 90 天档数量 >= 60 天档 >= 30 天档（累计） | ⬜ |
| 20 | 兼容旧字段 | `warranty_expiring_soon` 值 = 最大档位累计数 | ⬜ |
| 21 | 规格书覆盖率 | 配置修改—统计联动—Dashboard 显示全链路 | ⬜ |

**运行命令：**
```bash
cd ims-main/backend
uv run pytest tests/contract/test_warranty_alert_contract.py -v --tb=short
```

---

## v2.17 变更记录（2026-09-20 — 质保提醒配置化）

| # | 变更内容 |
|---|---------|
| 1 | 版本号 v2.16 → v2.17 |
| 2 | 新增 4.20：质保提醒配置测试（21项：配置接口12项 + 仪表盘联动9项） |
| 3 | 后端新增 `GET/PUT /settings/warranty-alert` 接口 |
| 4 | 后端 schemas 新增 `WarrantyAlertResponse` / `WarrantyAlertUpdate` |
| 5 | 后端 dashboard 响应扩展 `warranty_alert_enabled/days/by_day` |
| 6 | 前端 Settings.vue 新增"质保提醒"配置区域 |
| 7 | 前端 Dashboard.vue 质保卡片改为响应开关+分档显示 |
| 8 | 新增测试文件 `tests/contract/test_warranty_alert_contract.py`（21用例） |
| 9 | 质保周期不再硬编码，从 `sys_config` 读取 |


### 4.21 维修知识库功能测试（P2 — v2.17 新增）

> **目标**：验证维修知识库 CRUD、搜索推荐、工单沉淀全流程。
> **测试文件**：`tests/contract/test_rma_knowledge_base_contract.py`（已创建，2 P / 0 F / 5 s）
> **关联改善点**：改善点 #2（维修知识库）

| # | 场景 | 请求 | 预期 | 状态 |
|---|------|------|------|:---:|
| 1 | 创建知识库条目 | `POST /rma/knowledge-base` | 返回 200，含 `id` | ⬜ |
| 2 | 按关键词搜索 | `GET /rma/knowledge-base/search?keyword=无法开机` | 返回匹配条目列表 | ⬜ |
| 3 | 按故障码搜索 | `GET /rma/knowledge-base/search?fault_code=E001` | 返回精确匹配 | ⬜ |
| 4 | 无匹配结果 | `GET /rma/knowledge-base/search?keyword=xyz123` | 返回空数组 | ⬜ |
| 5 | 从维修工单沉淀 | `POST /rma/knowledge-base/from-repair/{repair_id}` | 自动填充故障描述和方案 | ⬜ |
| 6 | 引用计数递增 | 诊断时引用某条目 → `usage_count` +1 | 诊断后 usage_count 自增 | ⬜ |
| 7 | 诊断时自动推荐 | 提交诊断描述 → 返回 Top 3 相似案例 | 含匹配度评分 | ⬜ |
| 8 | 知识库条目发布 | `POST /rma/knowledge-base/{id}/publish` | 状态 DRAFT→PUBLISHED | ⬜ |
| 9 | 未登录访问 | `GET /rma/knowledge-base/search` without token | 返回 401 | ⬜ |

**运行命令：**
```bash
cd ims-main/backend
uv run pytest tests/contract/test_rma_knowledge_base_contract.py -v --tb=short
```

### 4.22 故障统计分析看板测试（P2 — v2.17 新增）

> **目标**：验证故障统计接口正确性和数据一致性。
> **测试文件**：`tests/contract/test_fault_statistics_contract.py`（已创建，2 P / 0 F / 6 s）
> **关联改善点**：改善点 #3（故障统计分析看板）

| # | 场景 | 请求 | 预期 | 状态 |
|---|------|------|------|:---:|
| 1 | 按型号统计故障 Top10 | `GET /rma/statistics/fault-by-model?top=10` | 返回降序排列，每项含 `sku_code`, `fault_count` | ⬜ |
| 2 | 按故障类型统计分布 | `GET /rma/statistics/fault-by-type` | 返回 `fault_code` + `count` + `percentage` | ⬜ |
| 3 | 按场站统计返厂频次 | `GET /rma/statistics/fault-by-station` | 关联 device_ledger 的 station | ⬜ |
| 4 | 月趋势统计 | `GET /rma/statistics/fault-trend?granularity=month` | 返回每月故障数 | ⬜ |
| 5 | 异常波动检测 | `GET /rma/statistics/anomaly-check` | 本月故障数 > 过去6月均值×2 标记异常 | ⬜ |
| 6 | 时间范围筛选 | `GET ...?start_date=2026-01-01&end_date=2026-06-30` | 仅返回时间范围内数据 | ⬜ |
| 7 | 无故障数据 | 空的 rma_return 表 | 返回空数组/0 值，不报错 | ⬜ |
| 8 | SQL对照验证 | 接口返回 vs `SELECT sku_id, COUNT(*) ... GROUP BY sku_id` | 数量一致 | ⬜ |

**运行命令：**
```bash
cd ims-main/backend
uv run pytest tests/contract/test_fault_statistics_contract.py -v --tb=short
```

### 4.23 供应商质量评估测试（P2 — v2.17 新增）

> **目标**：验证供应商质量评分算法和各指标计算正确性。
> **测试文件**：`tests/contract/test_supplier_quality_contract.py`（已创建，3 P / 0 F / 4 s）
> **关联改善点**：改善点 #4（供应商质量评估）

| # | 场景 | 请求 | 预期 | 状态 |
|---|------|------|------|:---:|
| 1 | 获取供应商质量评分列表 | `GET /incoming/supplier-quality` | 返回含 `acceptance_rate`, `defect_rate`, `delivery_avg_days`, `quality_score` | ⬜ |
| 2 | 合格率计算 | 10批到货，8批 ACCEPTED | `acceptance_rate=80.00` | ⬜ |
| 3 | 不良率计算 | defect_qty=5, sample_qty=100 | `defect_rate=5.00` | ⬜ |
| 4 | 交货周期计算 | delivery_date - created_at 平均值 | `delivery_avg_days` 正确 | ⬜ |
| 5 | 质量评分 | 合格率80、不良率5、准时率90 | `quality_score≈84`（80×0.5+95×0.3+90×0.2） | ⬜ |
| 6 | 低于60分预警 | 某供应商评分 < 60 | 标记预警 | ⬜ |
| 7 | 无到货记录供应商不出现在列表 | 新供应商无历史记录 | 不在结果中 | ⬜ |
| 8 | 时间范围筛选 | `?start_date=&end_date=` | 仅统计时间段内数据 | ⬜ |

**运行命令：**
```bash
cd ims-main/backend
uv run pytest tests/contract/test_supplier_quality_contract.py -v --tb=short
```

### 4.24 批量操作增强测试（P1 — v2.17 新增）

> **目标**：验证批量提交审核、批量分配、批量导出的正确性和事务一致性。
> **测试文件**：`tests/contract/test_batch_operations_contract.py`（已创建，4 P / 0 F / 5 s）
> **关联改善点**：改善点 #6（批量操作增强）

| # | 场景 | 请求 | 预期 | 状态 |
|---|------|------|------|:---:|
| 1 | 批量提交入库单 | `POST /inbound/orders/batch-submit {ids:[1,2,3]}` | 返回 `{success:[1,3], failed:[{id:2,reason:"xxx"}]}` | ⬜ |
| 2 | 全部成功 | 3张单据均符合条件 | `success=[1,2,3], failed=[]` | ⬜ |
| 3 | 部分失败不影响成功 | 第2张已提交 → 第1/3张仍应提交 | 事务独立 | ⬜ |
| 4 | 批量分配RMA任务 | `POST /rma/returns/batch-assign {ids:[1,2], assigned_to:3}` | 返回 200 | ⬜ |
| 5 | 导出带 ids 参数 | `GET /inbound/orders/export?ids=1,2,3` | 只导出指定 ID 的数据 | ⬜ |
| 6 | 大数据量（50条） | 批量提交50张单据 | 响应 < 5s | ⬜ |
| 7 | 权限检查 | WAREHOUSE 角色提交他人的单据 | 无法操作，列表中不出现 | ⬜ |

**运行命令：**
```bash
cd ims-main/backend
uv run pytest tests/contract/test_batch_operations_contract.py -v --tb=short
```

### 4.25 筛选条件记忆全覆盖测试（P1 — v2.17 新增）

> **目标**：验证 useFilterPersist 覆盖所有列表页，TTL 过期逻辑正确。
> **测试文件**：前端 Vitest 单元测试（待创建 — 前端 composable 需先实现 useFilterPersist）
> **关联改善点**：改善点 #7（筛选条件记忆全覆盖）

| # | 场景 | 检查方式 | 预期 | 状态 |
|---|------|---------|------|:---:|
| 1 | Stocktake 页接入 | 代码审查 | 含 `useFilterPersist('stocktake', ...)` 调用 | ⬜ |
| 2 | InventoryAdjustment 页接入 | 代码审查 | 含 `useFilterPersist('inventory_adjustment', ...)` | ⬜ |
| 3 | DeviceLedger 页接入 | 代码审查 | 含 `useFilterPersist('device_ledger', ...)` | ⬜ |
| 4 | BOM 页接入 | 代码审查 | 含 `useFilterPersist('bom', ...)` | ⬜ |
| 5 | Products 页接入 | 代码审查 | 含 `useFilterPersist('products', ...)` | ⬜ |
| 6 | Partners 页接入 | 代码审查 | 含 `useFilterPersist('partners', ...)` | ⬜ |
| 7 | Customer 页接入 | 代码审查 | 含 `useFilterPersist('customer', ...)` | ⬜ |
| 8 | Station 页接入 | 代码审查 | 含 `useFilterPersist('station', ...)` | ⬜ |
| 9 | Snapshot 页接入 | 代码审查 | 含 `useFilterPersist('snapshot', ...)` | ⬜ |
| 10 | Inventory 页接入 | 代码审查 | 含 `useFilterPersist('inventory', ...)` | ⬜ |
| 11 | TTL 过期 | 修改 _ts 为 8 天前 → 刷新页面 | 筛选条件恢复为默认 | ⬜ |
| 12 | reset 清除 | 点击重置按钮 | localStorage 中对应 key 被清除 | ⬜ |
| 13 | dateRangeShortcuts 传入 | 每个页面的 el-date-picker | `:shortcuts="dateRangeShortcuts"` | ⬜ |

**运行命令：**
```bash
cd ims-main/frontend
npx vitest run --reporter=verbose  # 前端单元测试
```

### 4.26 表单自动保存草稿测试（P0 — v2.17 新增）

> **目标**：验证 useFormDraft 自动保存、恢复、清除逻辑。
> **测试文件**：前端 Vitest 单元测试（待创建 — 前端 composable 需先实现 useFormDraft）
> **关联改善点**：改善点 #8（表单自动保存草稿）

| # | 场景 | 操作 | 预期 | 状态 |
|---|------|------|------|:---:|
| 1 | 自动保存 | 填写 RMA 维修工单 → 等待 2s | localStorage 中出现 `ims_draft_rma_repair` | ⬜ |
| 2 | 恢复草稿 | 关闭表单 → 重新打开 | 弹窗提示"是否恢复草稿" | ⬜ |
| 3 | 确认恢复 | 点击"恢复" | 表单字段恢复到上次填写的内容 | ⬜ |
| 4 | 忽略草稿 | 点击"忽略" | 表单为空，草稿清除 | ⬜ |
| 5 | 提交后清除 | 提交成功 → 重新打开 | 不应提示恢复草稿 | ⬜ |
| 6 | TTL 过期 | 修改 _ts 为 8 天前 → 打开表单 | 不应提示恢复 | ⬜ |
| 7 | excludeFields 排除 | 配置 `excludeFields: ['return_id']` | `return_id` 不保存到草稿 | ⬜ |
| 8 | 多表单互不影响 | 同时打开 RMA 诊断 + RMA 维修 | 两个草稿相互独立 | ⬜ |
| 9 | 不同用户隔离 | 用户A的草稿 vs 用户B的草稿 | 不同 key 前缀隔离 | ⬜ |
| 10 | localStorage 无敏感数据 | 检查草稿内容 | 不含密码/jwt token | ⬜ |

**运行命令：**
```bash
cd ims-main/frontend
npx vitest run --reporter=verbose
```

### 4.27 扫码连续录入测试（P0 — v2.17 新增）

> **目标**：验证 BarcodeScanner continuous 模式、去重逻辑和混合输入。
> **测试文件**：手动测试（扫码硬件依赖，backend 无对应契约测试）
> **关联改善点**：改善点 #9（扫码连续录入）

| # | 场景 | 操作 | 预期 | 状态 |
|---|------|------|------|:---:|
| 1 | 连续扫码5个SN | 打开 continuous 模式 → 连续扫5个不同SN | 弹窗底部显示"已扫5个"，列表含5行 | ⬜ |
| 2 | 去重 | 扫同一个SN两次 | 提示"SN已存在"，列表不重复 | ⬜ |
| 3 | 全部确认 | 扫完后点击"全部确认" | 表单 SN 列表追加5行 | ⬜ |
| 4 | 逐个删除 | 扫3个 → 删除第2个 → 确认 | 表单只含第1和第3个SN | ⬜ |
| 5 | 手动添加 | 扫码中途手动输入SN → 确认 | 混合模式可用 | ⬜ |
| 6 | 中途取消 | 扫3个 → 点击取消 | 表单不追加任何SN | ⬜ |
| 7 | Shipment 接入 | 出货单 SN 录入 | 使用 continuous 模式 | ⬜ |
| 8 | Inbound 接入 | 入库单 SN 录入 | 使用 continuous 模式 | ⬜ |
| 9 | Stocktake 接入 | 盘点扫码 | 使用 continuous 模式 | ⬜ |
| 10 | 性能（100个SN） | 连续扫100个SN | 弹窗不卡顿 | ⬜ |

### 4.28 打印模板可视化配置测试（P2 — v2.17 新增）

> **目标**：验证打印模板编辑器（若开发）的基本功能。
> **测试文件**：手动测试（打印模板编辑器需前端开发）
> **关联改善点**：改善点 #10（打印模板可视化配置）

| # | 场景 | 操作 | 预期 | 状态 |
|---|------|------|------|:---:|
| 1 | 新增模板 | 模板编辑器 → 填写名称/内容 → 保存 | 模板列表出现新模板 | ⬜ |
| 2 | 编辑模板 | 选择已有模板 → 修改 → 保存 | 修改生效 | ⬜ |
| 3 | 预览模板 | 点击预览 | 显示打印预览效果 | ⬜ |
| 4 | 删除模板 | 删除非默认模板 | 模板移除，打印列表不再出现 | ⬜ |
| 5 | 默认模板不可删除 | 尝试删除系统默认模板 | 提示不可删除 | ⬜ |
| 6 | 模板变量替换 | 模板中使用 `{{sn}}` `{{sku_code}}` | 打印时正确替换 | ⬜ |
| 7 | 兼容旧打印 | 保持现有6个Vue打印组件可用 | 旧打印功能不中断 | ⬜ |

### 4.29 设备生命周期追溯图谱测试（P1 — v2.17 新增）

> **目标**：验证 SN 全链路时间轴数据完整性和可视化。
> **测试文件**：`tests/contract/test_device_lifecycle_contract.py`（已创建，4 P / 0 F / 4 s）
> **关联改善点**：改善点 #11（设备生命周期追溯图谱）

| # | 场景 | 请求 | 预期 | 状态 |
|---|------|------|------|:---:|
| 1 | 获取SN全链路 | `GET /device-ledger/{sn}/lifecycle` | 返回按时间排序的事件列表 | ⬜ |
| 2 | 含生产信息 | 关联 BOM / ProductionTask | 事件含"生产完成"节点 | ⬜ |
| 3 | 含出货信息 | 关联 Shipment / Outbound | 事件含"出货"节点 | ⬜ |
| 4 | 含维修信息 | 关联 RMA Return / Repair | 事件含"维修"节点 + 维修前后SN | ⬜ |
| 5 | 含盘点信息 | 关联 Stocktake | 事件含"盘点"节点 | ⬜ |
| 6 | 含站址变更 | 关联 DeviceLedger.station_id 变更记录 | 事件含"站址变更"节点 | ⬜ |
| 7 | SN不存在 | `GET /device-ledger/INVALID_SN/lifecycle` | 返回 404 | ⬜ |
| 8 | 无事件设备 | 新建设备无任何历史 | 返回空事件列表 | ⬜ |
| 9 | 前端时间轴渲染 | 打开设备详情 → 生命周期Tab | 时间轴可视化正常 | ⬜ |

**运行命令：**
```bash
cd ims-main/backend
uv run pytest tests/contract/test_device_lifecycle_contract.py -v --tb=short
```

### 4.30 消息通知与待办推送测试（P1 — v2.17 新增）

> **目标**：验证 DingTalk/企微 webhook 通知和 FastAPI BackgroundTasks 机制。
> **测试文件**：`tests/contract/test_notification_contract.py`（已创建，3 P / 0 F / 5 s）
> **关联改善点**：改善点 #12（消息通知与待办推送）

| # | 场景 | 操作 | 预期 | 状态 |
|---|------|------|------|:---:|
| 1 | 待办通知触发 | 创建入库单 → 提交审核 | 触发 BackgroundTasks 发送通知 | ⬜ |
| 2 | 通知失败不阻塞业务 | webhook URL 不可达 → 提交入库单 | 入库单正常提交，通知静默失败 | ⬜ |
| 3 | 通知在事务提交后发送 | 通知查询数据库 | 能查到已提交的单据 | ⬜ |
| 4 | DingTalk webhook 签名 | 配置 secret → 发送 | HMAC-SHA256 签名正确 | ⬜ |
| 5 | 通知模板正确 | 入库单待审通知 | 含单据号、申请人、时间 | ⬜ |
| 6 | 质保到期通知 | warranty_end ≤ today+30天 | 通知含设备SN、到期日 | ⬜ |
| 7 | 通知频率限制 | 同一单据5分钟内多次提交 | 只发一次通知（去重） | ⬜ |
| 8 | 配置开关 | 关闭通知配置 | 不发送任何通知 | ⬜ |

**运行命令：**
```bash
cd ims-main/backend
uv run pytest tests/contract/test_notification_contract.py -v --tb=short
```

### 4.31 数据归档清理测试（P2 — v2.17 新增）

> **目标**：验证数据归档策略和清理脚本。
> **测试文件**：`tests/contract/test_data_archive_contract.py`（已创建，6 P / 0 F / 5 s）
> **关联改善点**：改善点 #13（数据归档清理）

| # | 场景 | 操作 | 预期 | 状态 |
|---|------|------|------|:---:|
| 1 | 数据摸底 | 运行 `scripts/data_audit.py` | 产出 `{表名: {行数, 月增量, 索引查询时间}}` 报告 | ⬜ |
| 2 | 按时间归档 | 归档2年前已完成的入库单 | 原表记录移入 `xxx_archive` 表 | ⬜ |
| 3 | 归档数据可查询 | `GET /inbound/orders/archive?start_date=` | 能查到归档数据 | ⬜ |
| 4 | 归档不丢关联 | 归档入库单 → 关联的检验记录同步归档 | 外键引用完整 | ⬜ |
| 5 | 归档后原表查询性能 | `EXPLAIN SELECT * FROM inbound_order WHERE ...` | 查询行数减少，扫描更快 | ⬜ |
| 6 | 归档脚本幂等 | 重复运行归档脚本 | 不报错，不重复归档 | ⬜ |
| 7 | 回滚验证 | 从归档恢复 | 数据完整恢复到原表 | ⬜ |

**运行命令：**
```bash
cd ims-main/backend
uv run python scripts/data_audit.py           # 数据摸底
uv run python scripts/data_archive.py --dry-run # 归档预演
uv run pytest tests/contract/test_data_archive_contract.py -v --tb=short
```

### 4.32 移动端适配增强测试（P2 — v2.17 新增）

> **目标**：验证超出现有 @media 覆盖的移动端增强功能。
> **测试文件**：Chrome DevTools 手动测试 / 真机测试（前端+Capacitor，backend 无对应契约测试）
> **关联改善点**：改善点 #14（移动端适配增强）

| # | 场景 | 操作 | 预期 | 状态 |
|---|------|------|------|:---:|
| 1 | 现有@media覆盖 | 检查 `src/style.css` | `@media (max-width: 768px)` 含弹窗/表格/表单/按钮 | ✅ 已实现 |
| 2 | 底部导航栏 | 移动端视口 | 固定底部导航（首页/扫码/待办/我的） | ⬜ |
| 3 | 手势操作 | 列表页下拉刷新 | 触发数据重新加载 | ⬜ |
| 4 | 离线缓存 | 断网 → 打开已访问页面 | 显示缓存内容 + "离线模式"提示 | ⬜ |
| 5 | Capacitor 原生扫码 | App端打开扫码 | 调用原生相机扫码（非浏览器 Html5Qrcode） | ⬜ |
| 6 | 推送通知 | App后台 → 收到待办通知 | 系统通知栏显示 | ⬜ |
| 7 | 横屏适配 | 旋转设备到横屏 | 布局不错乱，表格可用 | ⬜ |
| 8 | 触控区域 | 按钮/链接间距 | 符合 48×48dp 最小触控区域 | ⬜ |

### 4.33 U9深度集成测试（P2 — v2.17 新增）

> **目标**：验证 Phase1 U9 任务单号下拉选择和 Phase2 数据对接。
> **测试文件**：`tests/contract/test_u9_integration_contract.py`（已创建，4 P / 0 F / 3 s）
> **关联改善点**：改善点 #5（U9深度集成）

| # | 场景 | 操作 | 预期 | 状态 |
|---|------|------|------|:---:|
| 1 | 历史U9单号接口 | `GET /shipment/u9-task-nos` | 返回去重排序的 u9_task_no 列表 | ⬜ |
| 2 | 下拉可搜索 | 出货单 u9_task_no 字段改为 el-select | filterable 搜索可用 | ⬜ |
| 3 | 手动输入新值 | 输入下拉列表中不存在的新单号 | 可手动输入并保存 | ⬜ |
| 4 | U9 API连通性 | `GET /system/u9/health` | 返回 U9 接口连通状态 | ⬜ |
| 5 | 数据推送（Phase2） | 创建 Shipment → 自动推送 U9 | U9 收到出货数据 | ⬜ |
| 6 | 库存对账（Phase2） | `GET /system/u9/inventory-reconciliation` | 返回 IMS vs U9 库存差异 | ⬜ |
| 7 | 推送失败重试（Phase2） | U9 不可达 → 稍后恢复 | 自动重试3次，记录失败日志 | ⬜ |

**运行命令：**
```bash
cd ims-main/backend
uv run pytest tests/contract/test_u9_integration_contract.py -v --tb=short
```

### 4.34 CI/CD流水线+自动化测试（P2 — v2.17 新增）

> **目标**：验证 GitHub Actions 流水线的自动化测试和构建。
> **测试文件**：`.github/workflows/ci.yml`（已创建，待推送后验证）
> **关联改善点**：改善点 #15（CI/CD流水线）

| # | 场景 | 触发条件 | 预期 | 状态 |
|---|------|---------|------|:---:|
| 1 | Push触发测试 | `git push` 到 main/develop | 自动运行 pytest + vitest | ⬜ |
| 2 | PR检查 | 创建 Pull Request | 自动运行测试 + 代码检查 | ⬜ |
| 3 | 后端测试全通过 | CI 执行 `uv run pytest tests/ -q` | 所有测试通过 | ⬜ |
| 4 | 前端测试全通过 | CI 执行 `npx vitest run` | 所有测试通过 | ⬜ |
| 5 | Docker镜像构建 | 测试通过后 | 自动构建 Docker 镜像 | ⬜ |
| 6 | 测试失败阻断合并 | 某测试失败 | PR 不允许合并 | ⬜ |
| 7 | 测试报告产出 | CI完成 | 生成测试报告 + 覆盖率 | ⬜ |
| 8 | 数据库迁移检查 | CI 执行 `alembic upgrade head` | 迁移脚本无错误 | ⬜ |

**运行命令：**
```bash
# 本地模拟 CI 流程
cd ims-main/backend && uv run pytest tests/ -q --tb=short
cd ims-main/frontend && npx vitest run
```

---

## v2.17 变更记录（2026-09-20 — 15项改善点全覆盖）

| # | 变更内容 |
|---|---------|
| 1 | 版本号 v2.16 → v2.17 |
| 2 | 新增 4.20：质保提醒配置测试（21项） |
| 3 | 新增 4.21：维修知识库功能测试（9项） |
| 4 | 新增 4.22：故障统计分析看板测试（8项） |
| 5 | 新增 4.23：供应商质量评估测试（8项） |
| 6 | 新增 4.24：批量操作增强测试（7项） |
| 7 | 新增 4.25：筛选条件记忆全覆盖测试（13项） |
| 8 | 新增 4.26：表单自动保存草稿测试（10项） |
| 9 | 新增 4.27：扫码连续录入测试（10项） |
| 10 | 新增 4.28：打印模板可视化配置测试（7项） |
| 11 | 新增 4.29：设备生命周期追溯图谱测试（9项） |
| 12 | 新增 4.30：消息通知与待办推送测试（8项） |
| 13 | 新增 4.31：数据归档清理测试（7项） |
| 14 | 新增 4.32：移动端适配增强测试（8项） |
| 15 | 新增 4.33：U9深度集成测试（7项） |
| 16 | 新增 4.34：CI/CD流水线+自动化测试（8项） |
| 17 | 15项改善点检查项合计：138项 |